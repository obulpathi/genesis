default:
	echo "Tell me what to do"

install:
	sudo apt-get install git vim zsh tree shred

installall:
	sudo apt-get install git vim zsh tree shred

configure:
	echo "Configure script ... take a backup of files before overriding them"
